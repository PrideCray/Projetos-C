#include <stdio.h>
#include <string.h>

int main () {
    int sorteio();
    int n = sorteio();
    char str[15];
    char letra[15];
    int ret;
    int i = 0;

    switch(n)
    {
        case 0:
        strcpy(str, "cachorro");  
        printf("1");
        break;
        
        case 1:
        strcpy(str, "terra");
        printf("2");
        break;
                
        case 2:
        strcpy(str, "chipset");       
        printf("3");
        break;
               
        case 3:
        strcpy(str, "uatizapi");
        printf("4");
        break;
                
        case 4:
        strcpy(str, "pikomon");   
        printf("5");
        break;
    }
    printf("Informe uma letra: ");
    scanf("%s", &letra);
    
    ret = strcmp(str, letra);
    
    for(i = 0; i<=8; i++)
    {
       

    }
    
    if(ret < 0) {
    printf("As letras não coincidem");
    } else if(ret > 0) {
    printf("As letras não coincidem");
    } else {
    printf("As letras são iguais");
    }
    
    return(0);
}

int sorteio(){
    srand(time(NULL));
    int n = rand()%5;

    // n == número escolhido para selecionar a palavra.
    return n;
}
